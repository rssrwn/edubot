stages:
  - build
  - test
  - deploy_staging_server
  
#before_script:
#  - eval $(ssh-agent -s)
#  - echo "$SSH_PRIVATE_KEY" | tr -d ’\r’ | ssh-add - > /dev/null

build:
  stage: build
  script:
    - echo building

test:
  stage: test
  script:
    - echo testing
    
deploy:
  stage: deploy_staging_server
  script:
    - rm -rf frontend.tar.gz
    - tar -czvf frontend.tar.gz web
    - scp -o StrictHostKeyChecking=no frontend.tar.gz g1727114_u@shell2.doc.ic.ac.uk:/vol/project/2017/271/g1727114/
    - ssh g1727114_u@shell2.doc.ic.ac.uk "cd /vol/project/2017/271/g1727114/ &&  rm -rf frontend && tar -xzvf frontend.tar.gz && rm -rf frontend.tar.gz"
  only:
    - master
