stages:
  - build
  - test
  - deploy_staging_server
  
#before_script:
#  - eval $(ssh-agent -s)
#  - echo "$SSH_PRIVATE_KEY" | tr -d ’\r’ | ssh-add - > /dev/null

build:
  stage: build
  script:
    - echo building

test:
  stage: test
  script:
    - echo testing
    
deploy:
  stage: deploy_staging_server
  script:
    - cd ..
    - rm -rf project.tar.gz
    - tar -czvf project.tar.gz g1727114
    - scp -o StrictHostKeyChecking=no project.tar.gz g1727114_u@shell2.doc.ic.ac.uk:/vol/project/2017/271/g1727114/
    - ssh g1727114_u@shell2.doc.ic.ac.uk "cd /vol/project/2017/271/g1727114/ && tar -xzvf project.tar.gz && rm -rf project.tar.gz && ls && pwd && mv g1727114/* ."
  only:
    - master
